import 'package:flutter/material.dart';

final miTema = ThemeData.dark().copyWith(
  canvasColor: Color.fromARGB(132, 0, 0, 255),
  bottomNavigationBarTheme: const BottomNavigationBarThemeData(
    selectedItemColor: Colors.red,
    )
);